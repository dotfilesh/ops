---
# Dummy secret — copy to secret.sops.yaml and encrypt with:
#   task encrypt FILE=kubernetes/apps/monitoring/kube-prometheus-stack/secret.sops.yaml
#
# Grafana admin credentials (if not using SSO).
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: monitoring
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "CHANGE-ME-dummy-grafana-pass"
