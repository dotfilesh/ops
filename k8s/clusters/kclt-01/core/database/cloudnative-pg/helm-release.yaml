---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cloudnative-pg
  namespace: database
spec:
  interval: 15m
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.18.1
      sourceRef:
        kind: HelmRepository
        name: cloudnative-pg
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    # this is the only reason that CNPG is a 📁 core item instead of
    # an 📁 apps one. CRD installation is a prerequisite and the
    # way the CNPG repository is set up, it is difficult to install
    # the CRDs within 📁 crds as they normally would. Please see #42
    crds:
      create: true
