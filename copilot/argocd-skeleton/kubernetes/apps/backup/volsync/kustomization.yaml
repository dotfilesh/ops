---
# kubernetes/apps/backup/volsync/kustomization.yaml
# Source plan: ops-rebuild-plan.md §2026 — backup/volsync
# Volsync provides PVC replication and snapshot-based backup to S3 (Ceph RGW).
# Chart version: 0.10.x  (ops-rebuild-plan.md §2026)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helm-release.yaml
