---
# kubernetes/apps/auth/authentik/kustomization.yaml
# Source plan: ops-rebuild-plan.md §2026 — auth/authentik
# Deploys Authentik (identity provider) via Helm in the auth namespace.
# Authentik version: 2024.12.x (ops-rebuild-plan.md §2026)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helm-release.yaml
  # Uncomment when secrets are created:
  # - secret.sops.yaml

# KSOPS generator for Authentik secrets (age-encrypted, ops-rebuild-plan.md §2026)
# generators:
#   - secret-generator.yaml
