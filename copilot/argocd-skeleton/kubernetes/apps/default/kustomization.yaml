---
# kubernetes/apps/default/kustomization.yaml
# Source plan: ops-rebuild-plan.md §2026 — Namespace: default
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - echo-server/
