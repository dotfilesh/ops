---
# kubernetes/apps/default/echo-server/kustomization.yaml
# Source plan: ops-rebuild-plan.md §2026 — default/echo-server
# echo-server is used as a health check / connectivity test endpoint.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helm-release.yaml
